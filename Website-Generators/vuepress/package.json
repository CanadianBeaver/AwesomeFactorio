{
  "scripts": {
    "dev": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress dev src --host localhost --open",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress build src --dest build",
    "deploy": "vuepress build src --dest build"
  },
  "devDependencies": {
    "vuepress": "1.9.2"
  }
}
